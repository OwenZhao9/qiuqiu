[project]
name = "qiuqiu-models"
version = "0.1.0"
description = "模型能力适配层"
requires-python = ">=3.12,<3.13"
dependencies = [
    "httpx>=0.27",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["qiuqiu_models"]
