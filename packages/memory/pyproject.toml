[project]
name = "qiuqiu-memory"
version = "0.1.0"
description = "记忆中间件与人格层"
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["qiuqiu_memory"]
